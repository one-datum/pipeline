$schema: "http://json-schema.org/draft-07/schema#"
description: Configuration file for noise model inference

type: object
properties:
  seed:
    type: integer
  min_nb_transits:
    type: integer

  num_color:
    type: integer
  num_mag:
    type: integer

  num_optim:
    type: integer

  color_smoothing_scale:
    type: number
  mag_smoothing_scale:
    type: number
